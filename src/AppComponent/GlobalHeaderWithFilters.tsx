import { PageHeader, type PageHeaderProps } from "@/components/design-system/page-header";

export default function GlobalHeaderWithFilters(props: PageHeaderProps) {
  return <PageHeader {...props} />;
}
